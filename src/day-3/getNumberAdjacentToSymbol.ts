/**
 * Adjacent contemplate up, down, left, rigth and diagonal
 * dot (".") is not a symbol
 */
export const getNumberAdjacentToSymbol = (schematic: string[]): number[] => {
    schematic.forEach(element => {
        console.log(element)
    });
	return [0]
}